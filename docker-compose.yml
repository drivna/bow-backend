version: "3.9"
services:
  flask-app:
    build:
      context: .
      dockerfile: Dockerfile.flask
    container_name: bow-backend-flask-app
    ports:
      - "4000:4000"
    environment:
      - FLASK_ENV=production
      - FLASK_APP=run.py
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=local
      - DB_NAME=bow_db
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      - QDRANT_URL=http://qdrant:6333
    volumes:
      - .:/app
    depends_on:
      - db
      - redis
      - qdrant
    networks:
      - flask-network

  bow-worker-2:
    container_name: bow-worker-2
    build:
      context: .
      dockerfile: Dockerfile.worker
    restart: always
    environment:
      - FLASK_ENV=production
      - FLASK_APP=run.py
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=local
      - DB_NAME=bow_db
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      - QDRANT_URL=http://qdrant:6333
    volumes:
      - .:/app
    depends_on:
      - db
      - redis
      - qdrant
    networks:
      - flask-network

  bow-socket-server:
    build:
      context: ../bow-ws
    container_name: bow-socket-server
    ports:
      - "4001:4001"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - SOCKETIO_REDIS_DB=1
      - SESSION_REDIS_DB=0
    volumes:
    - ../bow-ws:/app
    depends_on:
    - redis
    - flask-app
    networks:
    - flask-network



  redis:
    image: redis:7-alpine
    container_name: bow-redis
    ports:
      - "6379:6379"
    networks:
      - flask-network

  db:
    image: postgres:15
    container_name: postgres-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: local
      POSTGRES_DB: bow_db
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - flask-network

  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant-db
    ports:
      - "6333:6333"
    volumes:
      - qdrant_storage:/qdrant/storage
    networks:
      - flask-network

networks:
  flask-network:
    driver: bridge

volumes:
  postgres_data:
  qdrant_storage:
